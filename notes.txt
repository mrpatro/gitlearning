hello, this is my first file in the repository
wow thats incredible

brew
	install manager
cat: 
	reads the contents of the file
ls
	list the files /contentns of the dir
pwd
	current path
sleep 
	sleeps for n seconds
git init
	initialise the git repository
git status
	status of the files
git add file name
	adding the files to the container
git commit
	staged to repository

git diff
	says about files changed in pwd and not yet in stages
git log
	says about the log details of the commits/ shows the versions of the commits
git checkout <commit_id>
	moving to the working dir of that version(commit id)

